(function( $ ) {
 
  $.fn.validateField = function() {

    return this.each(function() {
        var fields = $(this).valid();
        // var isValid = fields.data("isValid", true);
        
        
    });
  };


}( jQuery ));
